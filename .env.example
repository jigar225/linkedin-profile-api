LI_AT=
JSESSIONID=

# ── server ───────────────────────────────────────────────────────────
PORT=8080

# Request rhythm toward LinkedIn. Unset = "human": section calls go out in
# waves with jittered pauses, mimicking the real web app. Set to "fast" for
# local development only — the all-at-once burst pattern is an automation
# fingerprint that risks the account.
# LINKEDIN_PACING=human

